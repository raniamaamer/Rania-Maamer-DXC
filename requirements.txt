# DXC Tunisia — Python dependencies
# Install: pip install -r requirements.txt

# ── Django Stack ──────────────────────────────────────────
django==4.2.16
djangorestframework==3.15.2
django-cors-headers==4.4.0
django-prometheus==2.3.1

# ── PostgreSQL ────────────────────────────────────────────
psycopg2-binary==2.9.9       # PostgreSQL adapter (binary, no build needed)

# ── Data / ETL ────────────────────────────────────────────
pandas==2.2.2
numpy==1.26.4
openpyxl==3.1.2              # Excel reading (.xlsx)
xlrd==2.0.1                  # Legacy .xls support


# ── Visualisation (Notebooks) ─────────────────────────────
matplotlib==3.8.4            # Graphiques EDA
seaborn==0.13.2              # Heatmaps + visualisations avancées

# ── Dev tools ─────────────────────────────────────────────
python-dotenv==1.0.1         # .env support
gunicorn==22.0.0             # Production WSGI server
whitenoise==6.7.0            # Static file serving in production
jupyter==1.0.0               # Notebooks Jupyter
ipykernel==6.29.4            # Kernel Jupyter pour VS Code

# ── Testing ───────────────────────────────────────────────
pytest-django==4.8.0
factory-boy==3.3.1

# ── Scheduling ────────────────────────────────────────────
django-apscheduler==0.6.2