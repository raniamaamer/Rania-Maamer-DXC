apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus        # ← doit être exactement "prometheus"
    url: http://prometheus:9090
    isDefault: true
    access: proxy